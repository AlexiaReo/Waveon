# Cloud Run environment variables for WaveOn backend
# Use with:
#   gcloud run deploy ... --env-vars-file backend/cloudrun-env.yaml

PUBLIC_BASE_URL: "https://waveon-backend-vo7jb6dnjq-ew.a.run.app"
JWT_SECRET: "WaveonJWT#2026-Super-Secret-Key-ChangeMe"
CORS_ALLOWED_ORIGINS: "https://waveonplay.web.app"

# Cloud SQL Postgres via socket factory (the `&` is safe in YAML)
DATABASE_URL: "jdbc:postgresql:///waveondb?cloudSqlInstance=waveonplay:europe-west1:waveon-db&socketFactory=com.google.cloud.sql.postgres.SocketFactory"
DATABASE_USERNAME: "waveonuser"
DATABASE_PASSWORD: "Waveon#2026-Strong-DB-Pass!"

